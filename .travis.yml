language: python
python:
  - "3.4"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: py.test --cov=instax tests_basic.py tests_image_encoder.py tests_premade.py --doctest-modules
after_success: coveralls
